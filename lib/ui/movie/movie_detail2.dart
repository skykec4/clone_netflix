import 'package:flutter/material.dart';

class MovieDetail2 extends StatelessWidget {
  const MovieDetail2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(

        child: Text('movie detail2'),
      ),
    );
  }
}
