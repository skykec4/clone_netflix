import 'package:flutter/material.dart';

class TvDetail extends StatelessWidget {
  const TvDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('detail'),
      ),
    );
  }
}
